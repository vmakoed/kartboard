<%= link_to 'Kartboard', root_path, class: 'text-gray-600 font-bold text-2xl' %>
<% if user_signed_in? %>
  <%= link_to 'Sign out', destroy_user_session_path, data: { turbo_method: :delete, turbo_action: 'replace_current', turbo_frame: '_top' }, class: 'bg-gray-200 text-gray-600 font-bold py-2 px-4 rounded text-lg' %>
<% end %>
